{
  "eval_id": 0,
  "eval_name": "new-project-setup",
  "prompt": "Node.js 24 + Express 5 + Drizzle ORM + better-sqlite3 + Vitest を使った新規バックエンドAPIプロジェクトをセットアップする手順を教えてください。package.json、tsconfig.json、Drizzle設定、Vitest設定の重要なポイントを含めてください。",
  "assertions": [
    {
      "text": "mentions pool:forks in vitest config",
      "check": "response mentions pool: 'forks' or pool:forks in vitest configuration"
    },
    {
      "text": "explains why pool:forks is required (native addon / better-sqlite3)",
      "check": "response explains that better-sqlite3 is a native addon that crashes under worker_threads, requiring pool:forks"
    },
    {
      "text": "mentions DB_FILE :memory: for tests",
      "check": "response mentions using DB_FILE: ':memory:' environment variable for test isolation"
    },
    {
      "text": "mentions programmatic migrate() in setup — not drizzle-kit CLI",
      "check": "response mentions calling migrate() programmatically in beforeAll/setup.ts rather than relying on drizzle-kit CLI for tests"
    },
    {
      "text": "drizzle.config.ts uses relative paths (not import.meta.dirname)",
      "check": "response shows drizzle.config.ts with relative path strings like './src/db/schema.ts' rather than import.meta.dirname, or explains the CJS compilation issue"
    },
    {
      "text": "mentions erasableSyntaxOnly in tsconfig",
      "check": "response mentions erasableSyntaxOnly: true in tsconfig.json"
    }
  ]
}
